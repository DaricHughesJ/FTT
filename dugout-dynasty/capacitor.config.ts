import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.darichughes.dugoutdynasty',
  appName: 'Dugout Dynasty',
  webDir: 'dist',
};

export default config;
