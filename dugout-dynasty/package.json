{
  "name": "dugout-dynasty",
  "private": true,
  "version": "0.1.0",
  "description": "Idle arcade baseball manager — build a franchise, develop prospects, climb the leagues.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "cap:add:android": "npm run build && npx cap add android && npx cap sync",
    "cap:add:ios": "npm run build && npx cap add ios && npx cap sync",
    "cap:sync": "npm run build && npx cap sync"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/cli": "^6.1.0"
  }
}
